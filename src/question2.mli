(* This is an *interface* file, which specifies the types of top-level
   functions exported from its analogue for usage and testing: do not touch! *)

val extractMin : 'a list -> 'a * 'a list

val extractSort : 'a list -> 'a list
